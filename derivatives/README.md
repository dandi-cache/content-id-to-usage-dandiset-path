# Derivatives

This directory contains the output data files generated by the `code/update.py` script.

The primary output is a YAML file (`<cache_name>.yaml`) which is committed to the `main` branch.

A minified and compressed JSON version (`<cache_name>.min.json.gz`) is committed to the `min` branch by the `code/minify.py` script.
