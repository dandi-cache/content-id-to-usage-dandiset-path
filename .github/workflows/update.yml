name: Update

on:
  schedule:
    - cron: "0 4 * * *"  # Run daily at 04:00 UTC, after the source cache has updated
  workflow_dispatch:

jobs:
  Update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets._GITHUB_API_KEY }}

      - name: Checkout source cache
        uses: actions/checkout@v4
        with:
          repository: dandi-cache/content-id-to-unique-dandiset-path
          path: sourcedata/content-id-to-unique-dandiset-path

      - name: Configure git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "envs/uv.lock"

      - name: Run update
        run: uv run --project envs python ./code/update.py sourcedata/content-id-to-unique-dandiset-path/derivatives

      - name: Upload YAML
        run: |
          git add .
          git commit --message "update" || true  # || true in case of no changes
          git push

      - name: Run minification
        run: uv run --project envs python ./code/minify.py

      - name: Upload minification
        run: |
          git push origin --delete min || true  # || true in case branch doesn't exist yet

          git checkout -b min
          git add .
          git commit --message "update min" || true  # || true in case of no changes
          git push --set-upstream origin min

  NotifyOnFailure:
    runs-on: ubuntu-latest
    needs: [ Update ]
    if: ${{ always() && failure() }}
    permissions: {}
    steps:
      - uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "CI Failure: DANDI Cache: Content ID → Usage Dandiset Path"
          to: cody.c.baker.phd@gmail.com  # Add more with comma separation (no spaces)
          from: dandi-cache
          body: "Please check the latest run of the DANDI Cache update: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
